export { getConfig, updateConfig, parseViewportConfig, resetConfig } from './config';
export { ViewportConfigSchema, GlobalConfigSchema } from './types';
export type { ViewportConfig, GlobalConfig } from './types';
